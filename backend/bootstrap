#!/bin/bash
# 阿里云 FC Custom Runtime 启动脚本
# 运行环境：Python 3.10+

set -e

# 安装依赖（如果使用代码包方式，依赖已打包在目录中可跳过）
# pip install -r requirements.txt -t ./

# 启动 FastAPI 应用（FC HTTP 触发器端口固定为 9000）
exec python -m uvicorn app_FC:app --host 0.0.0.0 --port 9000 --log-level info
